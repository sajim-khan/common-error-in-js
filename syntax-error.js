function bigNum (num) {
    if(num > 5){
        return true
    }
    else {
        return false;
    }
}

// another example
//const sum = a, b => a + b
const sum =( a, b) => a + b // error first braket 
const result = sum(3,54 )
console.log(result);

const student = {
    name : "Student", // comma na dile error hobe
    age : 34
}

